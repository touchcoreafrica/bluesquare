import React from "react";
import Image from "../Image/Image";

const CheckIcon = () => {
  return (
    <div className="flex items-center justify-center p-2 rounded-full bg-[#D1A55C26]">
      <Image alt={"checkIcon"} src={"/assets/checkIcon.svg"} />
    </div>
  );
};

export default CheckIcon;
